# AXIS — Brand Lock

**品牌名称：** `AXIS`  
**Bot：** `AXIS BOT`  
**研究模块：** `AXIS LAB`  
**标语：** `Signals without the noise.`

## 命名原则

所有用户可见品牌与项目文案统一使用 `AXIS`。

推荐内部命名：

```text
axis/
AXIS
AXIS BOT
AXIS LAB
AXIS Signal
AXIS Analysis
```

数据库默认名称：

```text
axis
```

环境变量、Schema、代码类名可使用 `AXIS` / `Axis` / `axis` 作为大小写变体。

Discord Category / Channel 使用当前 Blueprint 中的 Emoji + English 命名。

Logo 资产：

```text
assets/axis-logo.png
```
