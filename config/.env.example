# Compatibility pointer only. The canonical template is the repository-root
# `.env.example`; keep secrets in the ignored root `.env` or a deployment
# Secret Manager. Do not add real values to this file.
